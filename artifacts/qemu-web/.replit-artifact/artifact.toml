kind = "web"
previewPath = "/"
title = "QEMU Web Emulator"
version = "1.0.0"
id = "artifacts/qemu-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21758

[services.development]
run = "pnpm --filter @workspace/qemu-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/qemu-web", "run", "build" ]
publicDir = "artifacts/qemu-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21758"
BASE_PATH = "/"
